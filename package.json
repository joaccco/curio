{
  "name": "curio",
  "version": "0.1.0",
  "private": true,
  "description": "Cambia unos minutos de distraccion por una idea que valga la pena explorar.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "start": "node scripts/build.mjs && node scripts/serve.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "tailwindcss": "^3.4.10"
  }
}
